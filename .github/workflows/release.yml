name: Build and Release

on:
  push:
    tags:
      - 'v*' # Trigger workflow on tags matching v*, e.g. v1.0.0
  workflow_dispatch: # Allow manual trigger

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Create ZIP file
        run: |
          zip -r GoogleAccountSwitcher.zip . -x "*.git*" -x ".github/*" -x "assets/images/icon.svg" -x "*.md"

      - name: Build CRX file
        run: |
          mkdir temp_build
          unzip GoogleAccountSwitcher.zip -d temp_build
          npx -y crx3 -p GoogleAccountSwitcher.pem -o GoogleAccountSwitcher.crx temp_build/

      - name: Build XPI file (Firefox)
        run: |
          cp GoogleAccountSwitcher.zip GoogleAccountSwitcher.xpi

      - name: Release
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: |
            GoogleAccountSwitcher.zip
            GoogleAccountSwitcher.crx
            GoogleAccountSwitcher.xpi
          generate_release_notes: true
